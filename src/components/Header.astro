---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'header'> {
  headerTitle?: string;
  title?: string;
  description?: string;
  isCentered?: boolean;
}

const {
  description,
  headerTitle,
  title,
  isCentered = false,
  ...attrs
} = Astro.props;
---

<header
  class:list={[
    'mb-3 grid gap-y-3 tablet:mb-6',
    {
      'mb-4 gap-y-4 tablet:mb-8': isCentered,
    },
    attrs.class,
  ]}
  {...attrs}
>
  {
    headerTitle && (
      <p class='text-xxs uppercase text-gray-700 tablet:text-xs laptop:text-s'>
        {headerTitle}
      </p>
    )
  }
  <h1
    class:list={[
      'tracking-base laptop:tracking-xl text-l font-bold text-black tablet:text-xl laptop:text-2xl',
      {
        'tracking-l text-xl tablet:text-center tablet:text-3xl laptop:text-4xl':
          isCentered,
      },
    ]}
  >
    {title}
  </h1>
  {
    description && (
      <p
        class:list={[
          'tracking-l text-s text-gray-700 laptop:text-base',
          {
            'tablet:text-center': isCentered,
          },
        ]}
      >
        {description}
      </p>
    )
  }
</header>
